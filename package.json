{
  "name": "poker-chipless",
  "private": true,
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/server/dist/index.js",
    "test:e2e": "npm run build && npx playwright test --config apps/e2e/playwright.config.ts",
    "test:e2e:headed": "npm run build && npx playwright test --config apps/e2e/playwright.config.ts --headed"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
